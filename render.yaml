services:
  - type: web
    name: student-results-portal
    plan: free
    runtime: python
    buildCommand: './build.sh'
    startCommand: 'gunicorn results_portal.wsgi:application'
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: student-results-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true

databases:
  - name: student-results-db
    plan: free